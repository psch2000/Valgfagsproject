// Makes us able to easily make rectangles and store them in values
export class Rect {
    
    constructor(x, y, width, height){
        this.x = x;
        this.y = y;
        this.width = width;
        this.height = height;
    }
}